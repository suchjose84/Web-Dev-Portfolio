import { useEffect } from "react";

export default function ProjectsPage() {
    useEffect(() => {
        document.title = "Web Dev Portfolio | Projects"
    })

    return (
        <section>
            Portfolio page works.
        </section>

    );


}